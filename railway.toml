[build]
builder = "nixpacks"
buildCommand = "cd server && npm install"

[deploy]
startCommand = "cd server && node server.js"
healthcheckPath = "/"
healthcheckTimeout = 30

[deploy.ports]
port = 3000
http = true
